""" Enum types for specific validation """
from enum import Enum


class ApiConfigFields(Enum):
    """ Enum type to validate configuration fieldnames """
    DOCKER_COMPOSE_FP = 'docker_compose_fp'

